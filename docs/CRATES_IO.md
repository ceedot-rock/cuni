# crates.io

Cargo.toml is **0.1.11** with Bank in the description. Git tag **`cuni-bank-0.1.0`** is already cut.

**Not published on crates.io yet.** As of 2026-09-25 the crate name `cuni` is free on crates.io, and this environment has no `cargo login` credentials (`~/.cargo/credentials*` absent). Do not claim a crates.io release until `cargo publish` succeeds under an owned token.

When publishing: `cargo publish` from a clean master tip that matches 0.1.11.
