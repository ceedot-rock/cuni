# Freeze (2026-07-26)

**Build only:** hosted CuNi Studio — playground against **emit + `cuni check`**, with **Notelog** and **Critic Book**.

## In scope

- `playground/server.py` (API + books)
- `playground/` UI
- Hosting (Docker / Fly)
- Docs for Studio only

## Frozen (do not expand)

- New language features (unless Studio is blocked)
- Package registry implementation
- Pump / token / fee buyback automation
- Pinata, INGOT, fetchgate, Umbra, quant side quests
- Growth agents / press spam

Unfreeze only when Studio is live and the convert path (link → install → exactness) works without hand-holding.
