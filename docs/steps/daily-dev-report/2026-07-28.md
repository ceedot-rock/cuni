# Daily dev report — 2026-07-28

## Context

Aligned on **CuNi + Agent-Rider** focus (system prompt). Other projects maintenance.

## Completed today

1. **Pulled** origin/master (17 commits): Agent chat, quotas, Getting Started, Rider API design, publish prototype, Progress link, Fly deploy workflow.
2. **Set GitHub secret** `FLY_API_TOKEN` on `ceedot-rock/cuni` so Actions auto-deploy works.
3. **Deployed Studio** to Fly (`flyctl deploy --config fly.toml --remote-only`) — Progress badge + latest master live at https://cuni-studio.fly.dev/
4. **Ran publish prototype** on `examples/spend-control.cuni` → `examples/spend-control.publish.json` (exactness PASS).
5. **Studio Publish path (code):**
   - `POST /api/publish` — exactness gate → store Rider-shaped metadata under `/data/published/`
   - Play mode **Publish** button + `app.js` handler
6. **Established** living `docs/steps/10-STEPS.md` + this work log path.

## In progress / next

- Commit + push Publish UI so GH Actions redeploys Studio.
- Rider registration API implementation (Agent-Rider repo).
- Wire Studio publish → Rider POST when API exists.

## Human / blocked

- X @CoreXeroC still not API-connected for automated posts.
- SuperGrok on wrong account (user issue; not this host).

## Metrics

- Studio notelog: ~29 (low organic traffic).
- GH stars: 3 (last check).
